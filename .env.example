OPENAI_API_KEY=sk-REPLACE_ME
# Optional: Jev judgement layer (query rerank, ingest checks, staleness). Leave unset to disable.
# TYPESAFE_API_KEY=
# Block all OpenAI and Jev calls (scripts fail closed):
# BRAIN_PRIVACY_STRICT=1
# Other switches:
# BRAIN_JEV=off
# BRAIN_QUERY_DAEMON=off
# BRAIN_CONFIDENCE_THRESHOLD=0.45
