# raw/

Drop zone. Put PDFs, notes, screenshots, transcripts, clipped articles, anything here.

**This directory is policy-protected for the LLM.** The assistant reads from here but never generates content into this folder. All synthesis goes into `wiki/pages/`.

Run `/wiki-ingest <path-in-raw>` to process a file into the wiki.
